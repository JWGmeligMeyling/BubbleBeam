package nl.tudelft.ti2206.network;


public class ClientTest {
	private int port = 87432;
	private String local = "127.0.0.1";
	
	/*
	 * @Test public void clientConnectTest() { Host host = mock(Host.class);
	 * Client client = new Client(local, port); new Thread(host).run(); new
	 * Thread(client).run(); verify(host).connect(); }
	 */
}
